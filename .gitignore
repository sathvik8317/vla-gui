# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# Environment / secrets
.env

# Downloaded model weights
models/

# Downloaded eval benchmarks (read-only, never a training input)
benchmarks/

# Eval/run output — regenerated by tests, the orchestrator, and `vlagui eval`, not source
reports/
fixtures/

# Node (targets/ apps that end up needing a JS toolchain)
node_modules/

# Claude Code local session state, auto-generated handoffs, and internal notes
.claude/
HANDOFF.md
CLAUDE.md
