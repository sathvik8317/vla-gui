id: todomvc-03
app: todomvc
url: http://localhost:8081/
seed_actions: []
instruction: "Add a new todo item labeled 'Write tests' by typing it into the input field and submitting it."
task_assertion: "Write tests"
optimal_steps: 1
notes: >
  KNOWN GAP: this task's success depends on the "type" action submitting via
  Enter after typing (TodoMVC only adds the item on the Enter keydown, no
  submit button). orchestrate.py's act_node currently calls Executor.type()
  but never Executor.press("Enter") — see T-4.5-adjacent gap. This task will
  not pass under the current orchestrator regardless of model quality until
  that's implemented; included anyway so the harness has a task ready the
  moment act_node supports it, per FR-4 (type is a required action type).
